# Handover, 23 September 2026

Written at the close of the September working session. Read this, then `CLAUDE.md`, `docs/methods.md` and `docs/disclosure-control.md`, before changing anything.

## Where things are

- Repository: `~/projects/prism-r`, public at github.com/SGilr/prism-r, branch `main`, last commit `6fa9733`. Working tree clean and pushed.
- Live site: https://prism-r.howpreventionworks.com, Cloudflare Pages, deployed by direct upload from `site/dist`, not from git. The live site matches `main` as of this handover.
- Tags: `v0.4.1-refresh`, `v0.5.0-explorer`, `v0.5.1-suppression-architecture`. Raw data bundle: `raw-data-2026-09-16` (current, 138 files); `-09-05-2`, `-09-05`, `-09` superseded, notes edited, never rewritten.
- Data: YCS July 2026 edition (provisional); DfE exclusions 2024/25; DfE and Welsh looked-after children to March 2025; Home Office stop and search and arrests to March 2025; Census 2021; IoD 2025 and WIMD 2019.
- Python: `.venv` on 3.14.2. `make test-fast` 162 passed at close; `make test` is the full suite with the slow smoke test.

## Since the 4 September handover

1. Boundary files for the explorer restored and the bundle re-cut with date-stamped, suffixed tags (`raw-data-YYYY-MM-DD`, `-2`, `-3` on collision).
2. Welsh looked-after children re-ingested for the year ending March 2025 from the full StatsWales export, after the July release was found seven weeks late. Rule adopted: verify latest data at source, do not infer from cadence.
3. `verify.yml` runs weekly and on every bundle release, building from the published bundle offline and asserting byte-identical outputs; `make clean-processed` fixed so a from-scratch build actually works.
4. Legend on the remand-duration chart.
5. Methods evaluation by Dr Hope Kent, cited on the methods page. Confidence intervals switched from Wald to an exact conditional interval (`pipeline/exact_ci.py`). Cross-platform rounding at serialisation (`pipeline/serialise.py`, six decimals, interval bounds only); determinism claim corrected everywhere. Not a Corrections entry: no published figure was wrong.
6. 15 September scheduled refresh failed because the publisher renamed the YCS file; fetcher and ingest made tolerant; refresh PR #1 merged with July 2026 data.
7. Refresh job now cuts a draft bundle on each PR; `publish-bundle.yml` publishes it on merge. `ci.yml` is dispatched onto refresh branches and reports a `CI (dispatched)` commit status, since a workflow-opened PR raises no event.
8. Explorer: any area opens from `/explore?area=<geo_id>`, the panel reproduces the Lambeth worked example (same bar charts, same palette, per-group figures, national comparators, suppressed cells shown as text). Home page caption corrected.
9. HMI Prisons' Children in custody 2025-26 survey quoted on the national picture page under "Who is held on remand", with HMIP's own significance flags and the size-versus-certainty caveat. Quoted, not ingested; recorded in `docs/data-sources.md` under "Quoted, not ingested" with checksums.

## Standing rules, verbatim from the owner

- "Verify latest data at source, do not infer from cadence."
- Disclosure control is non-negotiable: counts below 6 suppressed and shown as "<6, suppressed for disclosure control"; a suppressed count must leave no published field from which it can be recovered; rates hidden where the denominator is below 100; every decision logged; source suppression propagated as inherited. Never emit, infer or reconstruct a suppressed value. Individual-level data is out of scope, always.
- Rates are always recomputed from counts at the level displayed, never averaged from a lower level's rates. Never fill a district with its county's value. A force-level indicator says so rather than showing the parent force's value as if local.
- Refresh PRs: no auto-merge. A published bundle is never rewritten in place.
- The Corrections section records defects in published figures only.
- House style: UK English, sentence-case headings, no em dashes anywhere including code comments, metric units, verified facts only, no LLM clichés. Stan is never "Professor".
- Commit attribution line: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

## Watch list

- 15 October 06:00 UTC: first scheduled refresh since the automation changes. Check that the PR opens, the draft bundle is cut, `CI (dispatched)` appears on the PR, and after merge that `publish-bundle.yml` publishes the draft and `verify.yml` goes green. The publish path has not yet run for real.
- `TREND_TO = "2026-05"` in `pipeline/compute_target.py` is held fixed at the commitment month by design; confirm with the owner if the tracker's trend window is ever questioned.
- Drafts from unmerged refresh PRs are deleted by hand. Manual sources (Census, IoD, Home Office, StatsWales, boundaries) are still bundled by hand.
- Low priority, untouched: em dashes in four page titles (`<title>` strings), a stale sprint line in `CLAUDE.md`, and the HMIP page labelling its 2025-26 workbooks "2024-25".
- HMIP's survey is annual, each September, and hand-quoted; the workbook's "last time" tab would support a year-on-year line next year.

## Working commands

```
cd ~/projects/prism-r
make test-fast
make build
cd site && npm run build && npx --yes wrangler pages deploy dist --project-name=prism-r --branch=main --commit-dirty=true
```

The dev server is `npm run dev` in `site/`, port 4321; `.claude/launch.json` in the repo has the entry for the desktop app's preview.
