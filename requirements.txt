# PRISM-R pipeline dependencies.
# Spec names Python 3.12 as the target runtime; this environment runs 3.14
# because 3.12 is not installed and every wheel below resolves on 3.14.
# See CLAUDE.md, working conventions.
pandas==3.0.3
openpyxl==3.1.5
numpy==2.4.5
geopandas==1.1.3
topojson==1.10
pytest==9.0.3
certifi==2026.04.22
